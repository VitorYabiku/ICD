Precisa instalar o [uv](https://docs.astral.sh/uv/) antes de rodar os scripts.

Rodar scripts a partir da raiz do repo:

```sh
uv run python -m ep2.clustering_for_sampling
uv run python -m ep2.main
uv run python -m ep2.scripts.json_tables_to_latex
```
